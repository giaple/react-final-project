export const initialMacro = [{
    name: 'Macro 1',
    profile: [],
    isSelected: true
}];

export const macroReducer = (state, action) => {
    switch (action.type) {
        default:
            return state;
    }
}